---
description: 
globs: 
alwaysApply: true
---
开发过程中，如果要运行开发服务器，请在项目根路径./bootstrap.sh --dev 。可以每次需要启动开发服务器，都通知及让用户来执行。
需要获取最新的、版本清晰的技术文档、代码样例时，调用context7
项目后端用uv构建，前端用pnpm管理依赖
.env文件无法读取，要查看可以通过grep或者cat来看。
.env里已有变量是填了有效值的：
DEBUG=True
APP_ENV=development
NEXT_PUBLIC_API_URL="http://localhost:8000/api"
AGENT_RECURSION_LIMIT=30
SEARCH_API=tavily
TAVILY_API_KEY={key}
NEXT_PUBLIC_SUPABASE_URL=https://idezkdxwnzyrzpmwuimj.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY={key}
SUPABASE_DB_PASSWORD={password}
DATABASE_URL=postgresql://postgres.idezkdxwnzyrzpmwuimj:${SUPABASE_DB_PASSWORD}@aws-0-ap-southeast-1.pooler.supabase.com:5432/postgres